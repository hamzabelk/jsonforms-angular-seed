import { LayoutChildrenRenderPropsPipePipe } from './layout-children-render-props-pipe.pipe';

describe('LayoutChildrenRenderPropsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LayoutChildrenRenderPropsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
